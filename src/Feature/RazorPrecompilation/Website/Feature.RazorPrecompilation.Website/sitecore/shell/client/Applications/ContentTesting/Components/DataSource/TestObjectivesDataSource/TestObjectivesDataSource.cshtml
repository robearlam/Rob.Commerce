@using Sitecore.ContentTesting.Components.DataSource
@using Sitecore.Mvc
@using Sitecore.Mvc.Presentation
@model RenderingModel
@{
    //var rendering = Html.Sitecore().Controls().GetUserControl(Model.Rendering);
    //rendering.Class = "sc-TestObjectivesDataSource";
    //rendering.Requires.Script("contenttesting", "TestObjectivesDataSource.js");

    //rendering.Attributes["data-sc-excludeItems"] = rendering.GetString("ExcludeItems", "excludeItems");
    //rendering.Attributes["data-sc-contentLanguage"] = Context.ContentLanguage.ToString();

    //var htmlAttributes = rendering.HtmlAttributes;
  var resolver = Html.Sitecore().Controls().GetParametersResolver(Model.Rendering);
  var rendering = new TestObjectivesDataSource(resolver);
  var output = new HtmlString(rendering.Render());
}

@output
