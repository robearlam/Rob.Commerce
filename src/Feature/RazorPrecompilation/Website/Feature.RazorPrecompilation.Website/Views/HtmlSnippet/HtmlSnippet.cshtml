@model Sitecore.XA.Feature.GenericMetaRendering.Models.HtmlSnippetModel

@{
    Layout = Sitecore.Configuration.Settings.GetSetting("XA.Foundation.Presentation.MetaComponentLayoutPath", "../SXA/Meta Component Layout.cshtml");
}

@if (string.IsNullOrWhiteSpace(Model.Html) && Sitecore.Context.PageMode.IsExperienceEditorEditing)
{
    <text>[Html field is empty]</text>
}
else
{
    @Html.Raw(Model.Html)
}